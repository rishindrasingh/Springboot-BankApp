apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: bankapp-pvc
  namespace: bankapp

spec:
  accessModes:
    - ReadWriteOnce

  #storageClassName: gp3
  
  storageClassName: standard


  resources:
    requests:
      storage: 10Gi


